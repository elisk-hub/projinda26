// Header component - displayed at the top of every page
function Header() {
  return (
    // header element styled with the "header" class in App.css
    <header className="header">
      <h1>TaskFlow</h1>
    </header>
  )
}

// Export so App.tsx can import and use this component
export default Header